name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: python -m pip install --upgrade pip ruff pytest fastmcp httpx
      - name: Internal ecosystem deps (not on PyPI — ecosystem tracks main)
        run: pip install "git+https://github.com/gaganjainse/shesh-audit.git@main"
      - run: ruff check src/ tests/
      - run: pytest tests/ -q
